Version 3.13:

- Отключена поддержка DPI через манифест проекта; управление масштабированием перенесено в код
- В проект добавлены два новых файла: dpi_utils.cpp и dpi_utils.h
- Реализовано вычисление коэффициента масштабирования интерфейса на основе DPI окна, монитора или системы (в зависимости от доступности API)
- Реализована обработка изменения DPI во время работы программы (WM_DPICHANGED, начиная с Windows 10)
- Переписана обработка сообщений WM_GETMINMAXINFO и WM_SIZE с учётом текущего масштаба
- Переписан код позиционирования и масштабирования элементов интерфейса (кнопки, контролы, списки, шрифты)
- Масштабирование корректно применяется также к меню и стандартным диалоговым окнам
- Масштабирование интерфейса на основе DPI реализовано для систем начиная с Windows XP

Version 3.12:

- Удалил папку "Backup" (осталась при конвертации проекта из устаревшего типа в новый)
- Удалил папку "Gl" (старые файлы "*.h", которые в проекте уже не используются)
- Удалил файл курсора "cursor1.cur" и его упоминания в проекте (в файле ресурсов "1.rc")
- В свойствах проекта отключил "Использование MFC" ("Использовать стандартные библиотеки Windows")
- Для конфигурации "Release" настроил пункт "Оптимизация всей программы" - поставил "Использовать создание кода во время компоновки" (стояло "Без оптимизации")
- Для конфигурации "Debug" и "Release" настроил "Инструменты манифеста"-"Вход и выход"-"Поддержка DPI" - выбрал "Поддержка высокого DPI" (чтобы элементы окна рисовались пиксель в пиксель, без масштабирования)
- Для конфигурации "Debug" (для "Release" уже стояло) настроил "Свойства конфигурации" - "С++" - "Создание кода" - "Библиотека времени выполнения" - выбрал "(/MTd)" (и для "Release" выбрано "(/MT)"), отсутствия зависимостей от DLL и совместимости с Windows XP
- Удалил файлы "sound_rec.vcproj", "sound_rec.vcproj.SEM-HOME..", "sound_rec.vcproj.SEM-NOTEBOOK.SEM.user" (остались от старой версии проекта, больше не нужны)
- Удалил файл "afxres.h" (не нужен при отключенном MFC)
- Редактирование файла ресурсов "1.rc" - удалил лишний мусорный код (остался от старого проекта и от бывшей поддержки MFC), заменил  include "afxres.h" на <windows.h>
- Cоздал в папке проекта подпапку "res" и переместил туда все файлы "*.ico"
- Удалил файлы "sound_rec.ncb" и "sound_rec.suo" из кореневой папки (там же где лежит "sound_rec.sln"), они остались от старой версии проекта, больше не нужны

- Добавил чекбокс "Показать журнал собитый" для отображения/скрытия журнала событий
- Изменил размеры списка журнала событий
- Перенёс и изменил размеры кнопки очистки журнала событий (теперь не сбоку, а внизу списка журнала событий)
- Добавил действие при нажатии кнопки кнопки очистки журнала событий + славиши SHIFT (вкл/выкл отображение клиппинга)

- Оптимизация кода
